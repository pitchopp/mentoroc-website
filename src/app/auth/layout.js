export const metadata = {
    title: {
        default: "Mentoroc",
        template: "%s | Mentoroc",
    }
}

export default function Layout({ children }) {
    return (
        <>
        {children}
        </>
    );
}